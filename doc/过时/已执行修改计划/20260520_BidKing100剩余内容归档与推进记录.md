# 20260520 BidKing 100 剩余内容归档与推进记录

归档时间：2026-05-20

## 1. 当前基线

- 当前有效依据：`README_最新文档导航.md`、`20260520_BidKing100还原剩余Equivalent收口执行计划.md`、`bidking_restore_class_matrix.md`、`bidking_restore_table_matrix.md`、`bidking_restore_uiwnd_matrix.md`、`bidking_restore_acceptance_matrix.md`、`20260520_BidKing100最终验收包.md`。
- 收口口径：clean-room Equivalent，不复制或分发原商业源码、原素材、原音频、真实支付或平台服务。
- 已锁定指标：52 表 Verified，44 Equivalent，5 Visual Substitute，3 Service Simulated，0 Manual Review Required；1256 原类 Mapped；80 UIWnd Mapped；M0-M10 Verified，M11/E12 Equivalent Closed。
- 当前仓库状态：历史批次曾在已有 Notice/Language 本地修改基础上推进；后续批次均先执行 `git status`，只叠加当前收口改动，不回滚用户或既有线程留下的变更。

## 2. 剩余内容总表

| 优先级 | 剩余内容 | 当前风险 | 收口标准 |
| --- | --- | --- | --- |
| P0 | 全验证链回归 | 等价账本已闭合，但每次 UI/数据生成改动后仍需重新跑固定命令 | `validate:bidking-compat`、server/web/match-core 测试、typecheck、web build、Playwright、`git diff --check` 通过 |
| P0 | Raw key/配置占位文案外露 | 用户已发现语言 key 直接出现在 UI，Notice/LanguageListen/WareHouse/ItemType 等页面仍需巡检 | 玩家可见页面不出现 `language*`、`text_*`、`voice_path_*`、`配置行...包装` 等内部 key 或占位句 |
| P0 | 主要 UIWnd 版面巡检 | 背包、藏品百科已出现对齐/滚动问题，其他复杂窗口可能仍有溢出 | 桌面与移动端截图中无明显遮挡、错位、横向溢出、按钮文字挤压 |
| P1 | 局外主流程可玩性复核 | 商店、市场、协会、邮件、任务、排行、活动、设置等已接入，但需要整链路点击复核 | 主要入口能打开、能执行关键操作、失败态有可读提示 |
| P1 | 替代/模拟边界复核 | Visual Substitute 与 Service Simulated 已锁定，但人工验收时容易被误判为缺口 | 后台、文档和 UI 说明保持一致，不把替代素材或支付模拟误报为未完成 |
| P2 | 构建体积与性能 | Web build 可能仍有大 chunk 警告 | 不阻塞功能验收；必要时拆分懒加载或记录为性能优化项 |
| P2 | 最终截图证据归档 | Playwright 冒烟已有基础证据，缺少覆盖所有高风险窗口的截图索引 | 形成可重复生成的截图集与失败定位说明 |

## 3. 本轮执行批次

### 批次 A：文档归档与验证入口

- 新增本文档，作为剩余内容检查单。
- 更新 `README_最新文档导航.md`，把本文档列入当前有效依据。
- 执行轻量验证：先跑与当前修改直接相关的 `@bitkingdom/web` system table 测试、typecheck、web build。
- 执行状态：已完成。`@bitkingdom/web` 单测 8 文件 18 测试通过，`npm run typecheck` 全工作区通过，Web build 通过但仍有大 chunk 警告。

### 批次 B：Raw key 与公告文案收口

- 保留 Notice 原表字段和 key，用运行层安全展示函数把 raw key、配置占位句和 HTML/富文本碎片过滤掉。
- 生成器补 Notice 可读包装文案，减少启动公告和系统提示对占位文案的依赖。
- 测试锁定：公告标题不再展示 `公告弹窗123`，正文不再展示 `text_Notice_*` 或 `配置行...包装`。
- 执行状态：已完成首批。设置、问题反馈、启动公告和引导浮层已改为安全展示；新增 Playwright 文本冒烟锁定设置/问题反馈不外露 raw key。

### 批次 C：UI 巡检推进

- 先复查用户已指出的背包和藏品百科，再扩展到设置、商店、任务、邮件、协会、市场、排行、活动。
- 优先修玩家可见问题：文字重叠、列表滚动条错位、详情面板压住卡片、按钮文本溢出、空状态难读。
- 每轮修复后补充测试或截图验证，并在最终回复中说明推进进度、剩余内容和下一步。
- 执行状态：桌面与移动首批完成。已把 15 个局外主窗口纳入 Playwright raw key 与溢出巡检：背包、藏品百科、商店、任务、邮件、协会、交易、拍卖行、排行、通行证、充值、好友、设置、问题反馈、竞买人。
- 本批修复：任务、邮件、协会、市场/拍卖、排行、活动/通行证/充值等玩家可见文案改走 `safeBidKingDisplayText`；服务端随机名与邮件模板增加 raw key 安全兜底；设置/引导详情长文本增加换行约束，防止内部路径和配置 key 溢出。
- 本批巡检结果：上述 15 窗口桌面脚本巡检 `raw=false`、`viewportOverflow=0`、`textOverflow=0`；`bidking-system-text.playwright.ts` 已升级为覆盖这些主窗口的回归用例。
- 移动端补充：390x844 视口下底部 2 列导航已改成自适应高度，背景层不再截获点击；藏品百科品质筛选菱形和商店单列布局已修正，15 窗口移动脚本巡检同样达到 `raw=false`、`viewportOverflow=0`、`textOverflow=0`。

### 批次 D：本批验证收口

- 固定验证链已重跑：`validate:bidking-compat`、`@bitkingdom/match-core` 60 测试、`@bitkingdom/server` 73 测试、`@bitkingdom/web` 18 测试、全工作区 typecheck、Web build、全 Playwright 4 用例。
- 移动端追加验证：`bidking-system-text.playwright.ts` 现在单用例覆盖 15 窗口 × 桌面/移动两组视口，并断言无 raw key、无根横向溢出、无非滚动文本溢出。
- Web build 仍只有既有大 chunk 警告，未出现新的构建错误。
- Playwright CLI skill 的 WSL Chrome 路径不可用，本批按仓库既有 Playwright 配置执行浏览器验证，避免引入额外环境依赖。

### 批次 E：局外主流程操作回归

- 新增 `bidking-outgame-actions.playwright.ts`，用真实浏览器和真实服务端 profile 串起局外操作：商店购买、邮件领取、协会加入/捐献、交易所上架/成交、充值下单/到账、礼包领取。
- 本批锁定的状态变化：`ShopItem 10001 -> Item 100104` 入库、`Mail 1001` 附件领取、协会积分增加、交易订单从 `listed` 到 `sold`、`Pay 1` 订单 completed、`GiftPackage 1` 已领取并发放奖励库存。
- 执行状态：已完成。全 Playwright 现在为 5 个用例，包含后台、BattleItem、局外操作流、桌面/移动 15 窗口文案与溢出巡检。
- 本批验证通过：`validate:bidking-compat`、`@bitkingdom/match-core` 60 测试、`@bitkingdom/server` 73 测试、`@bitkingdom/web` 18 测试、全工作区 typecheck、Web build、全 Playwright 5 用例、`git diff --check`。

### 批次 F：可见配置 Key 二次收口与房间/局内 Evidence Guard

- 2026-05-21 继续 E10 UIWnd Evidence：Playwright raw key 与布局检查抽成 `bidking-ui-guards.ts`，后续系统窗口、局外动作流、房间大厅和局内主界面共用同一套规则。
- 可见 raw key 守门已覆盖 `language* / text_* / voice_path_* / wh_* / itemName_* / itemDesc_* / tx_* / hero_* / UI/Prefab / WareHouse.* / [[...]]` 等配置、资源和奖励数组形态。
- `bidking-battle-item.playwright.ts` 已把稳定进房流程扩展到 `.room-ready-hall` 与 `.match-layout`，在真实开局前后断言无 raw key、无横向溢出、无非滚动文本截断，并生成房间大厅与局内截图证据。
- `bidking-outgame-actions.playwright.ts` 的每次主面板打开都会同步执行 raw key 与布局 guard，局外主流程不再只验证操作成功，也验证当前窗口玩家可见文本稳定。
- 本批修复局内长情报文本：掌眼 feed、公共/私有情报、线索复盘长句改为可换行展示，避免“按排序规则命中”“显示轮廓尺寸/显示品质”等说明被截断。
- 本批验证通过：`@bitkingdom/web` typecheck、Web 6 文件 14 单测、`bidking-system-text.playwright.ts`、`bidking-battle-item.playwright.ts`、`bidking-outgame-actions.playwright.ts`、`git diff --check`。

### 批次 G：房间大厅与局内移动端 Evidence 补强

- 2026-05-21 将 `bidking-battle-item.playwright.ts` 从单桌面用例扩展为桌面 `1366x768` 与移动 `390x844` 两组真实开局证据流。
- 两组视口都会完整执行真实 profile 创建、商店购买 BattleItem、装备道具、进入战前整备、确认房间大厅、开局、点击局内 BattleItem，并校验后台 `battle_item_used` 事件。
- 房间大厅 `.room-ready-hall` 与局内 `.match-layout` 在两组视口下都执行 raw key guard、横向溢出 guard、文本截断 guard，并分别生成截图证据。
- 本批修复移动端局内仓标签串截断：`MarketIntelPanel` 的仓库标签从单行省略改为自然换行，避免“商船 / 珠宝 / 四人局 / 4人 / 16-32件”在手机宽度下被截断。
- 本批验证通过：`@bitkingdom/web` typecheck、`bidking-battle-item.playwright.ts` 桌面/移动 2 用例、`git diff --check`。

### 批次 H：后台复审页桌面/移动 Evidence Guard

- 2026-05-21 将共享 raw key 与布局守卫接入 `bidking-admin-smoke.playwright.ts`，后台 `.admin-layout` 在桌面 `1280x720` 与移动 `390x844` 下均断言无 raw key、无根横向溢出、无非滚动文本截断。
- 移动后台用例补充等待配置 Parity、最终验收摘要、最终复审清单加载完成，避免只截图加载态，确保审查证据覆盖真实内容。
- 本批修复后台长 profile id / 英文状态在账本、对局卡、配置表、事件行中的单行溢出：后台网格子项补 `min-width: 0`，长文本改为可换行展示。
- 本批验证通过：`bidking-admin-smoke.playwright.ts` 桌面/移动 2 用例。

### 批次 I：首页入口 Evidence Guard

- 2026-05-21 将首页 `.bidking-home` 纳入 `bidking-system-text.playwright.ts`，桌面 `1640x900` 与移动 `390x844` 均断言无 raw key、无根横向溢出、无非滚动文本截断，并输出首页截图证据。
- 本批同步复扫 15 个局外主窗口与战前整备，确保首页 guard 接入后没有破坏既有 UIWnd 证据链。
- 本批验证通过：`bidking-system-text.playwright.ts` 3 用例。

### 批次 J：局外动作后动态面板 Evidence Guard

- 2026-05-21 将 `bidking-outgame-actions.playwright.ts` 的真实点击 helper 升级为动作成功后自动复查当前面板，购买、领取、捐献、寄售、成交、充值、礼包、同游、会馆告示、成员审批、地区资源、任务领奖等动态回写状态都会执行 raw key 与布局 guard。
- 打开面板与动作后复查共用同一 helper，避免只验证初始面板而漏掉接口回写后的按钮、库存、状态、账本和任务文本。
- 本批验证通过：`bidking-outgame-actions.playwright.ts` 2 用例。

### 批次 K：固定全验证链回归

- 2026-05-21 按当前收口计划完整执行固定验证链：`validate:bidking-compat`、`@bitkingdom/match-core`、`@bitkingdom/server`、`@bitkingdom/web`、全仓 `typecheck`、Web build、全量 Playwright、`git diff --check`。
- 验证结果：compat validation 通过；match-core 5 文件 60 测试通过；server 6 文件 73 测试通过；web 8 文件 18 测试通过；全仓 typecheck 通过；Web build 通过但仍保留既有 chunk 体积警告；全量 Playwright 9 用例通过。
- 全量 Playwright 当前覆盖后台桌面/移动、BattleItem 真实房间/局内桌面/移动、局外动作双流程、首页桌面/移动、15 个局外主窗口桌面/移动、战前整备桌面/移动。
- 本批未纳入提交的生成 PNG 继续作为素材评审对象保护，未混入代码/文档收口提交。

### 批次 L：生成藏品 PNG 增量素材验收

- 2026-05-21 对剩余 6 张生成藏品 PNG 做独立素材验收：`1055004 / 1055005 / 1056001 / 1056003 / 1056006 / 1056013`。
- 本批未重新全量生成美术，只对现有增量文件执行 `python tools/validate-bidking-item-art.py --item-id ...`；6 张均通过尺寸、透明边界、主体覆盖率、边缘留白和色键残留校验。
- 人工视觉抽查确认 6 张图均为透明底、主体完整、无明显裁切，风格保持古代珍宝包装，可作为当前 clean-room 替代素材提交。

### 批次 M：最终截图证据索引

- 2026-05-21 新增 `20260521_BidKing最终Playwright截图证据清单.md`，把全量 Playwright 9 用例生成的 42 张关键截图按复审场景归类。
- 截图索引覆盖后台桌面/移动、首页桌面/移动、15 个局外主窗口桌面/移动、战前整备桌面/移动、房间大厅/局内桌面移动、局外动作双流程。
- `20260520_BidKing100最终验收包.md` 已挂载截图清单入口；`README_最新文档导航.md` 已把截图清单纳入当前实现镜像。

### 批次 N：默认资金与原版资金档还原

- 2026-05-21 深查资金不足问题，确认当前实现存在两层偏差：局内 `gameConfig.rules.initialCash=100,000` 固定过低，局外新档案默认铜钱写死为 `12,000`。
- 原表依据已接入：`Constant.initial_points_chooses=[100,000,500,000,1,000,000,2,000,000,3,000,000]`、`Constant.item_budget_chooses=[10,000,50,000,100,000,200,000,500,000,1,000,000]`、`Constant.init_items` 的铜钱奖励行 `[1,1,2,000,000]`。
- 局内开局资金改为表驱动解析：默认使用 `1,000,000`，并按当前 BidMap 的 `RankMap.min_bid_range` 自动抬到可覆盖最高最低出价的原版档位；高压图如 `BidMap 2601` 会使用 `3,000,000`。
- 房间大厅、战前整备和开局 runtime 均展示/使用同一个 `RoomSnapshot.initialCash`；服务端会吸附并校验客户端传入的资金档，旧客户端不能把房间压回低于地图需求的资金。
- 局外新档案默认铜钱改为读取 `init_items` 的 `2,000,000`，Web 本地档案与 Server 权威档案保持一致；本批只落铜钱行，其他 `init_items` 道具/货币行继续保持后续独立验收，避免把库存类型边界混入资金修复。
- 本批验证已通过：`@bitkingdom/match-core` 相关 39 测试、`@bitkingdom/server` profile/room 相关 61 测试。

### 批次 O：常量运行层二次审计与初始物资补齐

- 2026-05-21 按“表 Verified 但 runtime 未引用”的口径扫 `Constant`，发现 `init_items` 非铜钱奖励、`init_head`、`bid_game_count_chooses`、`round_time_chooses`、`room_playernum_chooses`、`bid_rate_chooses`、`relief_fund_*`、`initial_warehouse_capacity` 等仍有硬编码或只展示不运行的风险。
- `init_items` 已从只读取铜钱扩展为读取全部初始库存奖励；新档案和旧档案迁移都会补一次 `bidkingStarterRewardsV1`，避免重复发放，同时保留玩家已消耗后的状态边界。
- 初始头像改为读取 `Constant.init_head=120000`，不再依赖 `Head[0]` 的偶然排序。
- 房间局数、轮时、人数上限、出价倍率、救济金和初始仓容量新增表驱动 helper；服务端拍卖阶段时长、默认局数、bot 上限和大厅席位显示开始读取这些 helper。
- 战前规则摘要新增原表局数/人数/轮时、出价倍率和救济金展示，方便后续复审时直接看出常量是否进入玩家可见层。
- 后续批次 P 已继续收口：`relief_fund_*` 主动领取、市场/拍卖行 `item_bid_* / auction_*`、`mail_max_count`、`cabinet_gaincoin / cabinet_rate / collection_counts_max` 和 `bid_fanli` 已进入运行层、服务端状态、前端展示和测试。

### 批次 P：经济常量与救济/拍卖细项收口

- 2026-05-21 新增 `marketRuleRuntime / economyRuleRuntime`，把 `item_fee_rate / item_bid_fax / item_bid_fee / item_bid_price_add / item_bid_time / item_bid_cost / item_bid_public_time / item_bid_slot_base / item_bid_slot_max / auction_time_limit / auction_slot_price / auction_counts / auction_price_notice / mail_max_count / collection_counts_max / cabinet_rate / cabinet_gaincoin / bid_fanli` 统一解释为可复用运行层。
- 市场/拍卖上架从旧的固定税费改为原公式：上架费按 `item_bid_cost` 和周期计算，成交手续费拆为 `item_fee_rate` 上架手续费 + `Item.transaction_tax_rate` 阶梯交易税，并记录 `listingFee / tax / listingCost / totalPrice / netPrice`。
- 市场状态机新增 `item_bid_slot_base=5` 基础槽位、`item_bid_slot_max=10` 上限、`auction_counts=100` 快照上限、`mail_max_count=100` 信箱满阻断、`auction_time_limit` 竞价窗和 `auction_price_notice` 高价提示阈值。
- 救济金已从“规则展示”推进为服务端可领取动作：按总资产低于 `relief_fund_limit=100000`、每日 `relief_fund_times=3`、奖励 `relief_fund_amount=[[1,1,100000]]` 发放，并写入 profile ledger。
- 收藏收益快照展示 `collection_counts_max / cabinet_rate / cabinet_gaincoin`，领取收益会遵守 `cabinet_gaincoin=10` 秒最短间隔。
- 核心竞拍结算接入 `bid_fanli=[10000,100]`：亏损超过 10,000 时按千分比返还，结算与交易账本均可审计。
- 前端市集/拍场展示原表周期、槽位、全服上限、竞价窗、加价步进、上架费和税费拆分；背包/珍阁面板新增救济金状态与领取按钮。
- 本批验证已通过：`validate:bidking-compat`、match-core 68 tests、server 78 tests、web 18 tests、server/web/match-core typecheck。

## 4. 固定验证命令

```bash
npm run validate:bidking-compat
npm test -w @bitkingdom/match-core -- --run
npm test -w @bitkingdom/server -- --run
npm test -w @bitkingdom/web -- --run
npm run typecheck
npm run build -w @bitkingdom/web
npm run test:playwright
git diff --check
```

## 5. 推进记录

- 2026-05-20：建立本归档，确认当前剩余主线从“表级 Equivalent”转为“玩家可见 UI/文案/截图验收”。
- 2026-05-20：本轮优先处理 Notice/Language raw key 与配置占位文案外露，并把背包、藏品百科继续列为高风险 UIWnd 巡检项。
- 2026-05-20：完成 Notice/Language 首批收口：`safeBidKingDisplayText` 复用到系统面板，启动公告、设置、问题反馈和引导浮层不再直接展示 raw localization key 或生成占位描述。
- 2026-05-20：新增 `bidking-system-text.playwright.ts`，真实浏览器打开设置和问题反馈，断言玩家可见区域不包含 `language*`、`languagelisten*`、`voice_path_*`、`text_Notice_*`、`guide_text_*`、`配置行...包装`。
- 2026-05-20：本轮验证通过：`validate:bidking-compat`、`@bitkingdom/web` 测试、`@bitkingdom/server` 测试 73 个、`@bitkingdom/match-core` 测试 60 个、全工作区 typecheck、Web build、全 Playwright 4 个用例、`git diff --check`。
- 2026-05-20：15 个局外主窗口完成桌面 raw key/溢出首批收口，任务、邮件、协会、市场/拍卖、排行、活动/通行证/充值等面板改用安全展示文案，服务端 `LanguageName` 与 Mail 模板不再向玩家暴露原始配置 key。
- 2026-05-20：15 个局外主窗口完成移动端补扫，修复首页底部模块不可完整点击、背景层命中截获、藏品百科品质筛选误溢出和商店移动双栏压缩问题；全 Playwright 4 用例通过。
- 2026-05-20：新增局外主流程操作型 Playwright，真实点击商店、邮件、协会、交易所、充值和礼包链路并断言服务端 profile 状态变化；全 Playwright 5 用例通过。
- 2026-05-21：Playwright raw key/布局 guard 抽为共享 helper，并接入系统文本、局外动作流、BattleItem 真实开局流程；房间大厅与局内主界面开始进入稳定截图证据链。
- 2026-05-21：修复局内掌眼 feed、市场情报和线索复盘长文本截断；新增 guard 抓到并验证 `.match-layout` 已无非滚动文本溢出。
- 2026-05-21：BattleItem 真实开局流程新增移动端视口；`.room-ready-hall` 与 `.match-layout` 已有桌面/移动双端截图和 guard 证据。
- 2026-05-21：固定全验证链已回归通过；当前剩余从“自动化缺口”转为生成 PNG 素材评审、截图归档和后续体验打磨。
- 2026-05-21：6 张剩余生成藏品 PNG 已完成局部校验和视觉抽查，作为独立素材批次提交。
- 2026-05-21：最终 Playwright 截图证据清单已建立，截图产物保持在 ignored test-results，可通过全量 Playwright 重跑再生。
- 2026-05-21：默认资金收口启动，局内资金档、地图最低出价需求、局外初始铜钱均改为从原表常量派生，避免角色/房间默认资金不足以支撑正常局。
- 2026-05-21：常量运行层二次审计开始补漏，已把 `init_items` 全初始物资、`init_head` 和房间规则选择类常量接入 helper 与 UI/服务端入口。
