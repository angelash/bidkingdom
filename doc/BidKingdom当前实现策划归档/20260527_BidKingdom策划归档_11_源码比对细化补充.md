# 20260527 BidKingdom 策划归档 11 源码比对细化补充

## 比对口径

本补充把 `doc/竞拍之王源码策划归档/20260527_竞拍之王源码策划归档_11_源码比对细化补充.md` 中的源码证据，继续落到 BidKingdom 工程的类型、运行时、服务端路由和前端表现文件。字段以原版协议、表类和 Unity 客户端消费方式为准；线上服务端不可见的内部算法按协议推断实现，并保持客户端可观测行为等价。

## 维度覆盖表

| 维度 | 原版证据 | 实现落点 | 固定口径 |
| --- | --- | --- | --- |
| 局内权威快照 | `GameData`、`GameUserData`、`GameSkillData` | `packages/shared/src/index.ts`、`packages/match-core/src/bidking/gameDataRuntime.ts`、`packages/match-core/src/match.ts` | 回合历史、开始/下一轮/结束协议和结算 replay 都由 `BidKingGameDataSnapshot` 承接 |
| 房间协议 | `RoomData`、Room 系列协议 | `packages/shared/src/index.ts`、`apps/server/src/domain/battle/roomLobbyRuntime.ts`、`apps/server/src/domain/battle/roomRoundRuntime.ts` | 房间展示使用 `RoomSnapshot`，局内权威仍回落到 `GameData` |
| 场景随机表现 | `BattleRandom_Main`、`GetRandomMapIds()`、最终 `MapId` | `packages/match-core/src/bidking/bidMapRuntime.ts`、`packages/match-core/src/bidking/compatRuntime.ts`、`packages/match-core/src/match.ts`、`apps/web/src/bidking/battle/useMatchDerivedState.ts`、`apps/web/src/bidking/battle/BattlePanels.tsx` | 仅匹配入口 `BidMap.map_group` 非空时在首轮 `intel` 内用 `openingCandidates` 播放场景轮选，不生成独立玩法阶段 |
| 情报暗牌表现 | `IntelligencePanel`、`BattleIntelligence_Main/2/3` | `packages/match-core/src/match.ts`、`apps/web/src/bidking/battle/useMatchDerivedState.ts`、`apps/web/src/bidking/battle/BattlePanels.tsx` | 每轮 `intel` 内用 `intelligenceChoices` 四张暗牌揭示地图公共情报 |
| 拍场和掉落 | `BidMap`、`RankMap`、`Drop`、`Item` | `packages/match-core/src/bidking/bidMapRuntime.ts`、`packages/match-core/src/bidking/compatRuntime.ts`、`packages/match-core/src/bidking/gameDataRuntime.ts` | `map_group` 解析实际子拍场；掉落按 `Drop` 递归和权重抽取；缺引用直接抛错 |
| 竞买人技能 | `Hero.cast_type`、`Skill`、`SkillEffect` | `packages/match-core/src/bidking/compatRuntime.ts`、`packages/match-core/src/bidking/skillTargeting.ts`、`packages/match-core/src/bidking/skillEffectRuntime.ts` | 回合开始自动触发，写入 `skillFeed`、私有/公共线索和 `GameSkillData` |
| 试宝令 | `BattleItem`、`Item.skills[0]`、`SkillEffect` | `packages/match-core/src/items.ts`、`packages/match-core/src/bidking/battleItemUseRuntime.ts`、`apps/server/src/domain/battle/roomActionRuntime.ts`、`apps/web/src/bidking/battle/battleItemUi.ts` | 只按 `BattleItem.id -> Item.id -> Item.skills[0] -> Skill -> SkillEffect` 源链解析 |
| Bot/AI | `RankAi`、`RankMap.role_spawn` | `packages/match-core/src/bots.ts`、`apps/server/src/domain/battle/roomBotRuntime.ts`、`apps/server/src/domain/economy/profileSendAuctionRuntime.ts` | Bot 只产生出价、停手、试宝令和表情；估值不可见部分按协议推断并写审计字段 |
| 送拍 | `SendAuctionData`、`SendAuctionGameData` | `apps/server/src/domain/economy/profileSendAuctionRuntime.ts`、`apps/server/src/services/profileService.ts`、`apps/server/src/routes/economyRoutes.ts`、`apps/web/src/bidking/api/bidkingApiClient.ts` | 委托创建/成交/回收生成协议快照，结算局写入 `SendAuctionGameDataSnapshot` |
| 邮件/商店 | `MailItemData`、`ShopStatusData`、`Mail`、`Shop`、`ShopItem` | `packages/shared/src/index.ts`、`apps/server/src/domain/profile/profileMailRuntime.ts`、`apps/server/src/domain/economy/profileCommerceRuntime.ts`、`apps/server/src/services/profileService.ts` | 邮件附件按奖励行落账；商店刷新/购买按 `Shop` 和 `ShopItem` 限制执行 |
| UIWnd/系统表 | `UIWnd`、`DirtyWords`、`ErrorCode`、`Language`、`Sound` | `packages/bidking-compat/src/uiWndRuntime.ts`、`apps/web/src/bidking/app/windowRegistry.ts`、`apps/server/src/domain/system/textGuard.ts`、`apps/server/src/domain/system/errorCodeCatalog.ts` | 窗口层级、遮罩、BGM、关闭行为和系统文本均由源表驱动 |

## 协议字段落点

| 协议/数据 | 字段 | 实现字段/函数 | 说明 |
| --- | --- | --- | --- |
| `GameData` | `MapId`、`Round`、`StockContainer`、`UserLog`、`HeroSkillLog`、`MapSkillLog`、`ItemSkillLog`、`NextRoundTime`、`RoundCanUseItemCount`、`GameCarryItemMax`、`GameGoldRateMax`、`GameType`、`ServerTime` | `BidKingGameDataSnapshot`、`buildBidKingGameDataSnapshot()` | `mapId` 取实际拍场；`nextRoundTime/serverTime` 为 Unix 秒；系统上限来自 `bidKingGameDataSystemLimitsForSkillIds()` |
| `GameUserData` | `UserUid`、`HeroCid`、`UseItemLog`、`PriceLog`、`IsStandDown`、`IsQuit`、`SimSelectItemList`、`SimBuffItemList`、`SelectItemList` | `BidKingGameUserDataSnapshot`、`buildGameUserLog()` | 出价日志来自本局历史和本轮 bid；道具日志来自 `battle_item_used` 事件；已用道具同步到 `selectItemList.isUsed` |
| `GameSkillData` | `SkillCid`、`HeroCid`、`MapCid`、`ItemCid`、`CastTime`、`CastRound`、`HitBoxList`、聚合统计字段 | `BidKingGameSkillDataSnapshot`、`buildSkillLogs()`、`skillFeedToGameSkillLog()`、`battleItemEventSkillLogs()` | 英雄、地图、道具技能分别写入 `heroSkillLog/mapSkillLog/itemSkillLog`；`hitBoxList` 按效果类目遮罩 |
| `S2C_33/S2C_37` | `GameData` | `bidKingSourceProtocolsForEvent('round_started')` | 首轮发送 `S2C_33_game_start_notify`，后续回合发送 `S2C_37_game_next_round_notify` |
| `C2S_34/S2C_35/S2C_119` | `GameUid`、`BidPrice`、`UserUid` | `submitBid()`、`bid_submitted` 事件协议引用 | 出价只在 `auction` 阶段接受；已出价或停手后不可重复提交 |
| `C2S_38/S2C_39` | `ItemCid`、`ItemSkillLog` | `useBattleItem()`、`battle_item_used` 事件协议引用 | 试宝令在 `intel/auction` 可用；竞价阶段必须在出价前 |
| `C2S_42/S2C_43` | `GameUid`、`ErrorCode` | `passAuction()`、`auction_passed` 事件协议引用 | 出价 `0` 和停手统一进入停手语义 |
| `S2C_45` | `WinUserUid`、`GameData`、`OldCollectionExp`、`NewCollectionExp`、`LossRecovery`、`UserSkillList` | `match_ended` 协议引用、`buildFinalSummary()`、`buildLossRecoveryByPlayerId()` | 最终 replay 使用 `roundHistory[].bidKingGameData`；亏损返利用 `bidKingBidLossRebateAmount()` |
| `RoomData` | `MapCid`、`RoomPlayernum`、`RoundTime`、`InitialBudget`、`BidRate`、`CanSelectHeros`、`CanSelectItems`、`GameData` | `RoomSnapshot`、`snapshotRoom()`、`validBidMapId()`、`validRole()`、`roomRoundRuntime.ts` | 房间人数和开局资金来自 `BidMap`；可选角色通过源 Hero 映射校验；局内数据由 `PlayerSnapshot.public.currentRound` 和 `GameData` 承接 |
| `SendAuctionData` | `Uid`、`MapCid`、`SlotId`、`StockData`、`SendTime` | `SendAuctionDataSnapshot`、`buildSourceSendAuctionData()` | 创建委托时锁定仓库藏品，生成源格式库存容器；`sendTime` 为 Unix 秒 |
| `SendAuctionGameData` | `Uid`、`MapCid`、`GameData`、`GameOverTime`、`UserSkillList` | `SendAuctionGameDataSnapshot`、`buildSourceSendAuctionGameData()`、`buildSendAuctionGameState()` | 成交/回收都生成一局送拍结果；`gameOverTime` 和 `gameData.nextRoundTime/serverTime` 为 Unix 秒；Bot 出价压力读取 `RankAi` |
| `MailItemData` | `ItemCid`、`ItemCount`、`ItemNo`、`ItemQuility` | `MailInboxItem.attachmentRewards`、`mailAttachmentRewards()`、`applyRewardRowsToProfile()` | 表附件按奖励行解析；自定义系统邮件同样按奖励行落账 |
| `ShopStatusData` | `ShopCid`、`NextRefreshTime`、`ShopItemList` | `BidKingShopStatusDataSnapshot`、`refreshShopRestock()`、`ShopPurchaseState`、`ShopRestockState` | 商店刷新池、购买次数和下一次刷新时间均记录在档案状态 |

## 表现流程固定点

| 表现 | 触发 | 数据 | 时长 | 实现 |
| --- | --- | --- | ---: | --- |
| 场景随机 | 匹配入口 `BidMap.map_group` 非空且首轮 `intel` 开始 | `currentRound.openingCandidates`，最后一项为选中拍场 | 1600 ms | `roundPresentationOverlay()` 返回 `battle_random`，`BattleRandomOverlay` 渲染候选和结果 |
| 情报暗牌 | 每轮 `intel` 开始，存在地图公共情报 | `currentRound.intelligenceChoices`、`currentRound.intelligenceClue` | 1600 ms | `roundPresentationOverlay()` 返回 `intelligence_panel`，`IntelligencePanelOverlay` 渲染四张暗牌 |
| 操作窗口 | `intel` 和 `auction` | `canUseBattleItem`、`canBid` | 由服务器阶段控制 | 动画不改变服务端阶段，不改变试宝令、出价和 Bot 调度 |

## 错误边界

| 场景 | 实现行为 |
| --- | --- |
| `coreMode=false` | `createMatch()` 抛出 `Only BidKing core mode is supported` |
| 玩家数不等于 `BidMap.bidder_number` | `createMatch()` 抛出人数错误 |
| `BidMap`、随机子拍场、开场候选、`RankMap`、`Drop`、`Item` 引用缺失 | 对应运行时抛错 |
| `Hero`、`Skill`、`SkillEffect`、`BattleItem` 源链缺失 | 技能或试宝令运行时抛错 |
| `RankAi` 行缺失或权重区间为空 | Bot 决策运行时抛错 |
| 回合缺 `auctionDurationMs` | `match.ts` 和 `roomRoundRuntime.ts` 抛错 |
| 邮件/商店/送拍配置不存在或条件不足 | 对应服务端运行时返回错误，不生成替代落账 |

## 核对结论

本轮补充后，策划文档对局内协议快照、房间协议、表现动画、技能/道具日志、RankAi、送拍、邮件、商店、UIWnd 和系统表的实现落点都有明确索引。后续新增或重构这些模块时，按本分册的“协议字段 -> 实现函数 -> 固定口径”核对，不引入第二套玩法规则。
