# BidKing 协议覆盖矩阵

日期：2026-05-23

范围：

- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/GameReflection.cs`
- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/SimgameReflection.cs`
- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/StockReflection.cs`
- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/ShopReflection.cs`
- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/AuctionhouseReflection.cs`
- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/SendauctionReflection.cs`
- 源码协议：`reverse/bidking/code/decompiled/Scripts/Protodata/WarehouseReflection.cs`
- 当前实现：`packages/shared/src/index.ts`
- 当前实现：`packages/match-core/src/match.ts`
- 当前实现：`packages/match-core/src/auction.ts`
- 当前实现：`apps/server/src/roomManager.ts`
- 当前实现：`apps/server/src/routes/economyRoutes.ts`
- 当前实现：`apps/server/src/domain/economy/profileMarketRuntime.ts`
- 当前实现：`apps/server/src/routes/profileRoutes.ts`
- 当前实现：`apps/server/src/routes/socialRoutes.ts`

## 状态口径

- `Equivalent`：核心字段、触发时机和可见性与源码协议等价，允许本项目包装文案不同。
- `Socket substitute`：行为由 Socket.IO 事件和 `matchSnapshot/eventLog/toast` 替代，未使用原 Protobuf 消息号。
- `REST substitute`：行为由 HTTP API 和 profile snapshot 替代，未使用原 Protobuf 消息号。
- `Partial`：存在同域状态或部分行为，但字段、时机、错误响应或状态机不完整。
- `Missing`：当前没有可执行入口或稳定状态承载。
- `Out of scope`：测试/GM/源码调试协议，当前用户可玩流程不要求实现。

## 总览

| 模块 | 源协议数量 | 当前覆盖 | 主要差距 |
| --- | ---: | --- | --- |
| GameReflection | 37 条消息 + 1 个数据类型 | 核心对局已通过 Socket.IO、`matchSnapshot`、`MatchEventLog.sourceProtocols` 覆盖出价、停手、道具、回合开始、结算；明拍当前价可见性已按源码锁定 | 传输层不是 Protobuf；投票、预设、测试施法、精准 `GameData` 实时出口仍不完整 |
| SimgameReflection | 训练局消息 | `C2S_128/156` 使用入口、`S2C_129/157.UpdateGameData`、`C2S_130/S2C_131` 刷新、`C2S_134/S2C_135` 奖励选择已结构化 | 未接真实 Protobuf；服务端生成 `GameSkillData` 和 `GameWinItemList` 最终抽取仍未复刻 |
| StockReflection | 24 条消息 | profile stock container 已保存 `sourceUid/boxPositionData/isLock`，商店、市场、寄拍、收藏柜会移动或锁定实体 | 缺原协议级任意移动、穿戴、拆分、合并、回收、手动锁/解锁入口 |
| ShopReflection | 13 条消息 | 买入、刷新、收藏/取消收藏、收藏列表状态由 REST/profile 覆盖 | 缺原 `ShopStatus` 响应结构和 `S2C_391` 实时刷新通知 |
| AuctionhouseReflection | 16 条消息 + 4 个数据类型 | 市场订单已锁定实体库存，并归档 `C2S_276` 源字段；新增 `AuctionHouseLanchItem` 源形列表、稳定 `LanchItemUid`、`C2S_274/278/280/282/284/304` REST 替代入口；竞价会维护 `MaxPrice`、个人出价日志和保证金退还；撤拍可用源 `ItemUid=LanchItemUid`；到期有最高出价时会成交给赢家并进入买入/卖出交易记录 | 仍是 REST/profile 替代；均价、解锁槽和过期列表态未按原协议完整实现 |
| SendauctionReflection | 8 条消息 | 寄拍创建、列表、回收、对局列表由 REST/profile 覆盖 | 字段入口不是 `SlotId/ItemList` 原形；结算含本项目 demo action；未使用原消息号 |
| WarehouseReflection | 4 条消息 | 仓库物品由 profile inventory/stock 和背包页展示；礼包/奖励可入仓 | `OpenBoxSelectItemList/OpenBoxItemList` 的开箱选择协议未按源形态实现 |

本轮关键结论：源码 `S2C_119_game_user_bid_price_notify` 字段只有 `UserUid`、`GameUid`，没有 `BidPrice`。所以“明拍只看到谁出了价，等当轮全部结算后才看到当轮金额”是源码一致行为；当前实现已通过测试锁住公共快照在 round feedback 之前不暴露金额。

## GameReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `S2C_33_game_start_notify` | `GameData` | `startNextRound` 创建首轮；`matchSnapshot` 推公共/私有状态；核心事件标记 `sourceProtocols: S2C_33`；归档 `StockContainer.Position/BoxPositionData` 已按源码行/列坐标输出 | Socket substitute | 未实时下发原形 `GameData`，`BidKingGameDataSnapshot` 主要在回放/归档链路产生 |
| `C2S_34_game_bid` | `Token, GameUid, BidPrice` | `submitBid({ amount })`；服务端调用 `submitBid(match, playerId, amount)` | Socket substitute | 传输层字段名不同；无 token 字段，身份来自 socket/session |
| `S2C_35_game_bid` | `ErrorCode` | 成功后广播 `matchSnapshot`；失败用 `toast` + `apiErrorEnvelope` | Socket substitute | 无独立 ack 响应体；错误码不是原 Protobuf 消息 |
| `S2C_119_game_user_bid_price_notify` | `UserUid, GameUid` | `bid_submitted` 事件追踪源协议；公共快照中当前轮 `bids` 仅显示玩家已出价，金额置 `0/undefined` 直到 feedback | Equivalent | 已确认没有价格字段；继续防止新 UI 使用内部 `round.bids.amount` |
| `S2C_37_game_next_round_notify` | `GameData` | 后续 `startNextRound` 广播 `matchSnapshot`；核心事件标记 `S2C_37`；归档 `StockContainer.Position/BoxPositionData` 已按源码行/列坐标输出 | Socket substitute | 同 `S2C_33`，不是原形实时 `GameData` |
| `C2S_38_game_use_item` | `Token, GameUid, ItemCid` | `useBattleItem({ itemId, targetPlayerId })`；校验装备、库存、阶段、冷却 | Socket substitute | 字段名不同；target 行为来自扩展 payload |
| `S2C_39_game_use_item` | `ErrorCode, ItemSkillLog` | 成功广播快照并写 `battle_item_used`；`BidKingGameDataSnapshot.itemSkillLog` 输出源形技能日志，`HitItemIndex/AllHit*/HitItemTotalPrice/TotalHitBoxIndex` 已按源码聚合参数口径生成 | Partial | 没有独立响应体；`ItemSkillLog` 不作为 socket ack 原样返回 |
| `C2S_40_quit_game` | `Token, GameUid` | `leaveRoom` / socket disconnect / `player_disconnected` | Partial | 源码 quit 与当前离房/断线语义未完全等价 |
| `S2C_41_quit_game` | `ErrorCode` | `toast` 或房间快照变化 | Partial | 无独立 quit 响应 |
| `C2S_42_game_stand_down` | `Token, GameUid` | `passAuction()` | Socket substitute | 行为等价为本轮停手；传输字段不同 |
| `S2C_43_game_stand_down` | `ErrorCode` | 成功广播快照；核心事件标记 `S2C_43` | Socket substitute | 无独立 ack 响应体 |
| `S2C_45_game_over_notify` | `WinUserUid, GameData, OldCollectionExp, NewCollectionExp, LossRecovery, UserSkillList` | `match_ended`、`finalSummary`、`bidKingReplay`、`lossRecoveryByPlayerId`、profile apply summary | Partial | 旧/新收藏经验和 `UserSkillList` 不是一条原形消息输出 |
| `C2S_88_get_game_log_list` | `Token, UserUid` | 后台 `listMatches/getMatchDetail`、round replay | REST substitute | 面向后台审计，不是用户协议 |
| `S2C_89_get_game_log_list` | `ErrorCode, GameLogList` | `AdminMatchDetail.rounds/events/transactions` | REST substitute | GameLogList 原结构未复刻 |
| `C2S_120_use_item_to_target_user` | `Token, GameUid, EmojiCid, TargetUserUid` | 道具可带 `targetPlayerId`；表情当前 `sendEmote` 无目标用户 | Partial | 目标玩家表情/投射类协议没有完整源形实现 |
| `S2C_121_use_item_to_target_user` | `ErrorCode` | `toast` / 快照 | Partial | 无独立响应 |
| `S2C_123_use_item_to_target_user_notify` | `GameUid, UserUid, TargetUserUid, EmojiCid` | 无完整目标用户通知；局内表情只广播玩家当前 emote | Missing | 需要补 target-user emote/action notify |
| `C2S_228_get_now_game_data` | `Token` | `requestSnapshot` | Socket substitute | 返回 `matchSnapshot`，不是原形 `GameData` |
| `S2C_229_get_now_game_data` | `ErrorCode, GameData` | `emitPersonalSnapshot` | Partial | 需要可选 `BidKingGameDataSnapshot` 实时出口才接近原形 |
| `S2C_231_game_user_offline_notify` | `GameUid, UserUid` | disconnect 写 `player_disconnected` 并广播 room/match | Partial | 事件名和 payload 不同 |
| `S2C_233_game_user_online_notify` | `GameUid, UserUid` | rejoin 写 `player_rejoined` 并广播 room/match | Partial | 事件名和 payload 不同 |
| `C2S_238_game_vote` | `Token, GameUid, SlotId` | 当前没有对局内 vote socket；战前地图/拍场选择是 `setSelectedBidMap` | Missing | 若要复刻随机仓/候选仓投票，需要增加投票状态机 |
| `S2C_239_game_vote` | `ErrorCode` | 无 | Missing | 同上 |
| `S2C_241_game_vote_notify` | `GameUid, UserUid, SlotId` | 无 | Missing | 同上 |
| `C2S_248_get_game_preset_list` | `Token` | profile 中有已装备角色/道具；无 preset list API | Partial | 缺独立预设槽列表 |
| `GamePresetData` | `SlotId, HeroCid, ItemList, Name` | `selectedHeroId/equippedBattleItems` 表达当前选择 | Partial | 缺多槽、命名、解锁态 |
| `S2C_249_get_game_preset_list` | `ErrorCode, GamePresetList` | 无原形响应 | Partial | 需要 profile preset snapshot |
| `C2S_250_set_game_preset` | `Token, GamePresetData` | `selectHero`、`equipBattleItems` 分散实现 | Partial | 不是一次性设置预设 |
| `S2C_251_set_game_preset` | `ErrorCode` | REST/Socket 操作各自返回 snapshot/toast | Partial | 无独立响应 |
| `C2S_252_unlock_game_preset_slot` | `Token` | 无预设槽解锁 | Missing | 需要 preset slot 经济规则 |
| `S2C_253_unlock_game_preset_slot` | `ErrorCode` | 无 | Missing | 同上 |
| `C2S_262_game_use_emoji` | `Token, GameUid, EmojiCid` | `sendEmote({ emote })`，接 Emoji 表、冷却、音效、角色限制 | Socket substitute | payload 使用字符串 key，不是 cid |
| `S2C_263_game_use_emoji` | `ErrorCode` | `toast` | Socket substitute | 无独立 ack 响应体 |
| `S2C_265_game_use_emoji_notify` | `GameUid, UserUid, EmojiCid` | `matchSnapshot.players[].emote/emoteSoundId/...` | Partial | 不是独立 notify；cid 未直接下发 |
| `C2S_266_set_user_remark` | `Token, UserUid, Remark` | `/api/social/friend/remark` | REST substitute | 作用域是好友备注，不是对局用户备注 |
| `S2C_267_set_user_remark` | `ErrorCode` | REST snapshot/error | REST substitute | 无原形响应 |
| `C2S_290_test_game_cast_skill` | `Token, GameUid, ItemCid, SkillCid, HeroCid, MapCid` | `bidKingSimTrainingTestSkillCastRequest` 已结构化除 Token 外字段 | Partial | 源码测试/GM 类协议，尚无玩家或 admin endpoint |
| `S2C_291_test_game_cast_skill` | `ErrorCode, ItemSkillLog, NewGameData, HeroSkillLog, MapSkillLog, SkillLog` | `bidKingApplySimTrainingTestSkillCastResponse` 已按 `Item -> Hero -> Map -> Skill` 顺序消费日志，并用 `NewGameData` 同步训练局道具状态 | Partial | 只消费服务端回包，不本地生成真实 `GameSkillData` |

## SimgameReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `C2S_128_sim_game_use_item` | `Token, ItemUid, TargetBoxId` | `bidKingUseSimTrainingSelectItem` 生成 `ItemUid/ItemCid/TargetBoxId` 请求；`skilltarget=8` 道具缺目标格会返回 `missing_target_box` | Partial | 未接真实 Protobuf/Token；真实 `GameSkillData` 仍由服务端生成 |
| `S2C_129_sim_game_use_item` | `ErrorCode, UpdateGameData` | `UpdateGameData -> bidKingApplySimGameDataUpdate` 只同步 `SimSelectItemList/SimBuffItemList`，随后按 `GetNoPlaySkills` 边界消费服务端日志 | Partial | 无独立网络 ack；日志只建消费边界 |
| `C2S_156_sim_game_use_buff_item` | `Token, BuffItemCid, TargetBoxId` | `bidKingUseSimTrainingBuffItem` 生成 `BuffItemCid/TargetBoxId` 请求；同样支持 `skilltarget=8` 目标格门禁 | Partial | 当前表中主动增益道具尚未出现目标格需求；仍未接真实 Protobuf |
| `S2C_157_sim_game_use_buff_item` | `ErrorCode, UpdateGameData` | 同 `S2C_129`，只按源码同步训练道具列表并等待服务端技能日志 | Partial | 无独立网络 ack；日志只建消费边界 |

## StockReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `C2S_4_get_all_stocks` | `Token` | `/api/profile` 返回 `stockContainers` | REST substitute | 不返回源 `StockContainers` 信封 |
| `S2C_5_get_all_stocks` | `ErrorCode, StockContainers` | profile snapshot 的 `stockContainers` | Partial | 字段名与容器分组不同；`sourceUid/boxPositionData/isLock` 已补齐，`boxPositionData` 已按 `X=行/Y=列` 源坐标输出 |
| `C2S_6_get_one_stock` | `Token, StockId` | 无单 stock route；客户端从 profile snapshot 过滤 | Partial | 需要独立查询或统一 stock endpoint |
| `S2C_7_get_one_stock` | `ErrorCode, StockContainer` | 无原形响应 | Partial | 同上 |
| `C2S_8_set_all_box_in_stock` | `Token, StockId, BoxItems` | profileStockRuntime 有布局/归位内部能力 | Partial | 缺用户可调用的全量摆放 API |
| `S2C_9_set_all_boox_in_stock` | `ErrorCode, StockContainer` | 无原形响应 | Partial | 同上 |
| `C2S_12_move_item` | `Token, StockId, BoxId, Rotate, ToStockId, ToBoxId` | 收藏柜、市场、寄拍会抽取/返还实体；无任意拖拽移动 route | Partial | 需要 stock move API 才接近源协议 |
| `S2C_13_move_item` | `ErrorCode, StockContainers` | 操作后 profile snapshot 更新 | Partial | 返回范围和错误码不同 |
| `C2S_14_wear_stock` | `Token, StockId, BoxId, WearStockId` | `equipBattleItems` 按 itemId 装备，并保存可选 stockId/boxId | Partial | 携带选择对外 `boxId` 已改为源码格子位置；当前入口仍不是 box-first |
| `S2C_15_wear_stock` | `ErrorCode, StockContainers` | `/api/battle/items/equip` 返回 profile snapshot | Partial | 响应结构不同 |
| `C2S_16_unwear_stock` | `Token, StockId` | `equipBattleItems` 可用新列表清空/替换 | Partial | 缺单件卸下协议 |
| `S2C_17_unwear_stock` | `ErrorCode, StockContainer` | profile snapshot | Partial | 响应结构不同 |
| `S2C_19_add_item_notify` | `ItemList` | 商店购买、奖励、对局结算、市场成交会写 profile inventory/stock 并推 `profileUpdated` | Partial | 没有独立 add item notify；ItemList 原形未统一 |
| `C2S_20_split_item` | `Token, StockId, BoxId, SplitCount` | inventory counter 支持数量，但 stock box 拆分无用户入口 | Missing | 需要可堆叠实体拆分 |
| `S2C_21_split_item` | `ErrorCode, StockContainers` | 无 | Missing | 同上 |
| `C2S_22_merge_item` | `Token, StockId, BoxId, ToStockId, ToBoxId` | 计数库存会合并；stock box 合并无用户入口 | Missing | 需要可堆叠实体合并 |
| `S2C_23_merge_item` | `ErrorCode, StockContainers` | 无 | Missing | 同上 |
| `C2S_24_recycle_item` | `Token, RecycleList` | `/api/cabinet/sell`、`sell-all` 可出售库存 | REST substitute | 源 recycle list 和 stock container 返回不完整 |
| `S2C_25_recycle_item` | `ErrorCode, StockContainers` | REST snapshot/error | Partial | 不返回原 StockContainers |
| `C2S_234_lock_item` | `Token, StockId, BoxId` | 市场/寄拍会自动锁定克隆实体 | Partial | 缺手动锁定入口 |
| `S2C_235_lock_item` | `ErrorCode` | 无独立响应 | Partial | 同上 |
| `C2S_236_unlock_item` | `Token, StockId, BoxId` | 市场取消/过期/寄拍回收会返还解锁实体 | Partial | 缺手动解锁入口 |
| `S2C_237_unlock_item` | `ErrorCode` | 无独立响应 | Partial | 同上 |
| `C2S_306_set_big_stock_items` | `Token` | profileStockRuntime 自动迁移/整理大仓布局 | Partial | 源协议触发语义不明，当前为内部兼容 |
| `S2C_307_set_all_stock_all_items` | `ErrorCode` | 无独立响应 | Partial | 需要确认源码调用点后再定 |

## ShopReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `C2S_46_get_all_shop_status` | `Token` | profile snapshot + Shop/ShopItem compat runtime 生成商店视图 | REST substitute | 无独立 all shop status endpoint |
| `S2C_47_get_all_shop_status` | `ErrorCode, ShopStatusList` | `shopRestocks/shopPurchases/shopCollections` | Partial | 原 `ShopStatusList` 未原形返回 |
| `C2S_48_get_one_shop_status` | `Token, ShopCid` | 商店页从本地 compat 表和 profile 状态筛选 | Partial | 无单店状态 route |
| `S2C_49_get_one_shop_status` | `ErrorCode, ShopStatus` | profile snapshot | Partial | 同上 |
| `C2S_50_buy_item` | `Token, ShopCid, ItemUid, ItemCount` | `/api/shop/buy { playerId, shopItemId }` | REST substitute | 入口以 ShopItem 行 id 为主，未暴露 `ItemUid/ItemCount` 原字段 |
| `S2C_51_buy_item` | `ErrorCode, ShopStatus` | REST 返回 profile snapshot/error | Partial | 不返回单店 ShopStatus |
| `C2S_242_shop_collect_item` | `Token, ItemCid` | `/api/shop/collect { collected: true }` | REST substitute | 字段名不同 |
| `S2C_243_shop_collect_item` | `ErrorCode` | REST snapshot/error | REST substitute | 无原形响应 |
| `C2S_244_shop_uncollect_item` | `Token, ItemCid` | `/api/shop/collect { collected: false }` | REST substitute | 字段名不同 |
| `S2C_245_shop_uncollect_item` | `ErrorCode` | REST snapshot/error | REST substitute | 无原形响应 |
| `C2S_246_shop_get_collect_item_list` | `Token` | profile `shopCollections` | REST substitute | 无独立 collect list endpoint |
| `S2C_247_shop_get_collect_item_list` | `ErrorCode, CollectItemList` | profile snapshot | Partial | 响应信封不同 |
| `S2C_391_shop_refresh_notify` | `ShopCid` | shop autofresh 在 profile snapshot/商店页刷新时体现 | Partial | 缺服务端主动刷新 notify |

## AuctionhouseReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `AuctionHouseLanchItem` | `LanchItemUid, ItemCid, NumberCid, No, StartLanchTime, EndLanchTime, DisplayPeriodEndTime, Price, MaxPrice, StartPrice, Count` | `AuctionHouseLanchItemSnapshot` 已按源字段输出；`LanchItemUid` 从 order id 稳定映射，时间转源码秒级 Unix 时间；`MaxPrice` 已由本地竞价状态维护 | Partial | 未出价时为 0；无出价过期后仍自动返还，不保留源码“过期待下架/重上架”列表态 |
| `C2S_274_get_auction_house_lanch_item_list` | `Token` | `/api/auction-house/lanch-items?playerId=`，登录保护，返回当前玩家活跃拍卖上架源形列表 | REST substitute | 传输层不是 Protobuf；只返回当前活跃 listed/locked 订单 |
| `S2C_275_get_auction_house_lanch_item_list` | `ErrorCode, LanchItemList, LanchMax` | `AuctionHouseLanchItemListSnapshot { errorCode, lanchItemList, lanchMax }` | Partial | `ErrorCode` 仅成功码 0；`LanchMax` 来自本地上架槽规则 |
| `C2S_276_lanch_item_to_auction_house` | `Token, StockId, BoxId, Price, LanchTime, StartPrice, ItemCount, BagItemCid` | `createMarketOrderForProfile()` 锁定实体库存，并写入 `sourceAuctionHouseLaunches[]`；`BoxId` 用源码格子位置 `position`，不是内部实体句柄 | Partial | 未使用 Protobuf/Token；`StockId` 当前按主仓 `5001` 归档，未来多仓需随实体来源保存 |
| `S2C_277_lanch_item_to_auction_house` | `ErrorCode` | REST snapshot/error | REST substitute | 无独立源形响应 |
| `C2S_278_get_auction_house_item_list` | `Token, ItemCid, IsDisplayPeriod, SortType, Page, PageSize, Reverse` | `/api/auction-house/items` 支持 `itemCid/isDisplayPeriod/sortType/page/pageSize/reverse`，登录保护 | REST substitute | 排序是本地推断；未接真实 Protobuf |
| `S2C_279_get_auction_house_item_list` | `ErrorCode, ItemInfoList, CurrentPage, TotalPage` | `AuctionHouseItemInfoSnapshot { errorCode, itemInfoList, currentPage, totalPage }`，`MarketOrdersSnapshot.sourceAuctionHouseItemInfo` 也输出同形列表；`MaxPrice` 已读取本地竞价最高价；有出价拍品到期后会成交并从活跃列表移除 | Partial | 展示期和无出价过期态受当前本地市场状态机限制 |
| `C2S_280_auction_house_bid_price` | `Token, ItemUid, Price` | `/api/auction-house/bid`，用 `ItemUid=LanchItemUid` 定位订单，按 `Price` 扣竞价保证金，刷新 `MaxPrice` 和个人 `AuctionHouseBidLog`；被超价者/取消会退还当前占用，到期有最高出价时转为成交款 | REST substitute | 传输层不是 Protobuf；真实服务端只在客户端源码可见 `ErrorCode`，保证金/退款细节为按客户端成功后扣款行为和拍卖占用语义建模 |
| `S2C_281_auction_house_bid_price` | `ErrorCode` | 返回 `ProfileSnapshot & { sourceAuctionHouseBidPrice: { errorCode, itemUid, price, bidLog } }` | REST substitute | 响应体比源码多带调试/同步字段；错误码仍是 REST error，不是源 `Error.Code` |
| `C2S_282_auction_house_unlanch_item` | `Token, ItemUid` | `/api/auction-house/unlanch-item`，用 `ItemUid=LanchItemUid` 映射当前玩家拍卖订单，撤拍后返还实体库存并退还当前最高出价保证金 | REST substitute | 传输层不是 Protobuf；过期后仍走本地自动 expired 返还，不保留源码“过期待下架/重上架”列表态 |
| `S2C_283_auction_house_unlanch_item` | `ErrorCode` | 返回 `ProfileSnapshot & { sourceAuctionHouseUnlanchItem: { errorCode, itemUid, orderId } }` | REST substitute | 响应体比源码多带同步字段；错误码仍是 REST error，不是源 `Error.Code` |
| `AuctionHouseTradeInfo` | `TradeTime, ItemCid, NumberCid, No, Price` | `AuctionHouseTradeInfoSnapshot { tradeTime, itemCid, numberCid, no, price }`，由 sold auction order 的 `sourceAuctionHouseTradeTime/sourceAuctionHouseTradePrice` 和藏品元数据生成 | Implemented | 传输层不是 Protobuf；历史只来自本地订单 |
| `C2S_284_get_auction_house_trade_info` | `Token` | `/api/auction-house/trade-info?playerId=`，登录保护，按当前玩家聚合买入/卖出拍卖成交记录 | REST substitute | 未使用 Protobuf/Token |
| `S2C_285_get_auction_house_trade_info` | `ErrorCode, TradeInfoInList, TradeInfoOutList` | `AuctionHouseTradeInfoListSnapshot { errorCode, tradeInfoInList, tradeInfoOutList }`；`TradeInfoInList` 对应买入，`TradeInfoOutList` 对应卖出 | REST substitute | 响应体额外包含 `generatedAt`；错误码仍是 REST error，不是源 `Error.Code` |
| `AuctionHouseItemPriceInfo` | `ItemCid, AvgPrice, Count` | 成交订单已记录成交价，可作为均价聚合输入 | Partial | 还未提供 `AvgPrice/Count` 聚合响应 |
| `C2S_292_get_auction_house_all_item_price_info` | `Token` | 无 | Missing | 同上 |
| `S2C_293_get_auction_house_all_item_price_info` | `ErrorCode, AllAuctionHouseItemPriceInfo` | 无 | Missing | 同上 |
| `C2S_302_unlock_auction_house_lanch_slot` | `Token, UnlockCount` | 上架槽位由常量/本地规则限制 | Missing | 缺付费/条件解锁上架槽动作 |
| `S2C_303_unlock_auction_house_lanch_slot` | `ErrorCode` | 无 | Missing | 同上 |
| `C2S_304_get_auction_house_bid_log` | `Token` | `/api/auction-house/bid-logs?playerId=`，按登录玩家聚合自己参与过的活跃拍卖出价 | REST substitute | 只覆盖活跃订单的个人出价日志；过期/成交历史日志仍未保留为源列表态 |
| `AuctionHouseBidLog` | `BidTime, BidPrice, LanchItem` | `AuctionHouseBidLogSnapshot { bidTime, bidPrice, lanchItem }`，其中 `lanchItem.maxPrice` 反映当前最高出价 | Partial | 真实服务端日志持久化/历史保留策略不可见；当前只按玩家维度保留每个拍品最新个人出价 |
| `S2C_305_get_auction_house_bid_log` | `ErrorCode, BidLogList` | `AuctionHouseBidLogListSnapshot { errorCode, bidLogList }` | REST substitute | 非 Protobuf；成交/过期后的日志清理与源码 UI 的过期列表交互仍待补 |

## SendauctionReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `C2S_294_send_auction` | `Token, SlotId, MapCid, ItemList` | `/api/send-auction { mapCid, itemSelections: stockId/boxId[] }` | REST substitute | `SlotId` 由服务端分配/推断；`ItemList.BoxId` 已兼容源码格子位置和旧本地实体 id，内部归档改存源码格子位置 |
| `S2C_295_send_auction` | `ErrorCode, SendAuctionData` | REST 返回 `SendAuctionState` 和 profile snapshot | Partial | `SendAuctionData` 字段不是原形 |
| `C2S_296_get_send_auction_list` | `Token` | `/api/send-auctions` | REST substitute | includeHistory 是本项目扩展 |
| `S2C_297_get_send_auction_list` | `ErrorCode, SendAuctionDataList` | `{ generatedAt, auctions }` | Partial | 响应信封不同 |
| `C2S_298_recycle_send_auction` | `Token, SlotId` | `/api/send-auction/action { action: recycle, slotId }` | REST substitute | 字段包裹不同 |
| `S2C_299_recycle_send_auction` | `ErrorCode` | REST snapshot/error | REST substitute | 无原形响应 |
| `C2S_300_get_send_auction_game_list` | `Token` | `/api/send-auction/games` | REST substitute | 仅当前玩家范围 |
| `S2C_301_get_send_auction_game_list` | `ErrorCode, SendAuctionGameDataList` | `{ generatedAt, games }`，含 `gameData/finalPrice/profit` | Partial | 响应信封不同；`gameData.stockContainer.stockBoxes` 已按源码 `BoxId=position, Position.X=row, Position.Y=column` 输出 |

## WarehouseReflection

| 协议 | 源字段 | 当前实现 | 状态 | 差距/下一步 |
| --- | --- | --- | --- | --- |
| `C2S_82_get_warehouse_item_list` | `Token` | `/api/profile` 返回 `inventory/stockContainers`；背包页按 WareHouse runtime 展示 | REST substitute | 无独立 warehouse item list endpoint |
| `S2C_83_get_warehouse_item_list` | `ErrorCode, ItemList` | profile snapshot | Partial | `ItemList` 原形未统一 |
| `C2S_84_use_warehouse_item` | `Token, ItemCid, ItemCount, OpenBoxSelectItemList` | 礼包/活动/邮件/商店奖励进入库存；背包页有展示动作态 | Partial | 缺使用仓库道具、可选奖励箱选择协议 |
| `S2C_85_use_warehouse_item` | `ErrorCode, OpenBoxItemList` | 奖励领取返回 profile snapshot | Partial | 缺 `OpenBoxItemList` 原形输出 |

## 本轮代码修复

- `packages/shared/src/index.ts` 新增 `BidKingProtocolMessageRef`，`MatchEventLog` 增加 `sourceProtocols`，用于把当前事件追踪到源码协议号、名称、方向和字段。
- `packages/match-core/src/match.ts` 在核心模式下为 `round_started / bid_submitted / auction_passed / battle_item_used / match_ended` 附加源协议引用。
- `packages/match-core/src/bidking/compatRuntime.test.ts` 增加协议追踪测试，并新增明拍可见性回归：提交后公共快照只显示已出价，不显示金额；round feedback 生成后才显示本轮金额。
- `apps/server/src/domain/profile/profileStockRuntime.ts` 增加源码格子坐标转换：profile 层 `ItemData.BoxPositionData` 统一输出 `X=行/Y=列`，战斗道具实体选择返回源码 `BoxId=position`，旧本地实体 id 仍可消费。
- `apps/server/src/domain/economy/profileMarketRuntime.ts` 为市场上架订单增加 `sourceAuctionHouseLaunches` 源协议归档，对齐 `C2S_276_lanch_item_to_auction_house` 字段；内部 `lockedStockBoxes[].boxId` 继续作为实体生命周期句柄，源协议 `BoxId` 单独写 `position`。
- `apps/server/src/domain/economy/profileMarketRuntime.ts` 新增 `AuctionHouseLanchItemSnapshot` 兼容输出、稳定 `sourceAuctionHouseLanchItemUid`、`buildAuctionHouseLanchItemListSnapshot()` 和 `buildAuctionHouseItemInfoSnapshot()`，覆盖 `S2C_275/S2C_279` 主要字段。
- `apps/server/src/routes/economyRoutes.ts` 新增 `/api/auction-house/lanch-items` 与 `/api/auction-house/items`，并在 `serverApp` 中按源码 `Token` 语义纳入登录保护。
- `apps/server/src/domain/economy/profileSendAuctionRuntime.ts` 将送拍条目和回放 `StockContainerData` 改成源码 `BoxId=position`，并兼容旧选择输入。
- `apps/server/src/roomManager.ts` 创建匹配时把携带战斗道具的 `boxId` 转成源码格子位置。

## 剩余修复队列

1. P1：逐行复核 `SkillEffect` 到 `BattleItemEffectPlan` 的 category/target/cooldown/hitBox 输出，补缺失技能语义。
2. P1：补 target-user 类表情/道具通知，至少让 `C2S_120/S2C_123` 有可追踪替代实现。
3. P1：补游戏预设槽 profile 状态，使 `GamePresetData` 不再只是分散在 hero/equipped items。
4. P2：补 stock 任意移动、拆分、合并、手动锁/解锁 API，收口 `StockReflection` 的 Missing 项。
5. P2：补独立 shop/warehouse status endpoint 或在 review snapshot 中输出原形信封，降低 REST 替代差距。
